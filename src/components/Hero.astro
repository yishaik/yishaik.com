---
import type { Locale } from '../i18n/index';
import { heroContent } from '../content/homepageContent';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const isHe = locale === 'he';

const headline = isHe
  ? 'בונה והוגה מערכות — מ־Customer Success ועד AI יישומי.'
  : 'Building and operating systems — from Customer Success to applied AI.';

const ctas = isHe
  ? { primary: 'דבר איתי', secondary: 'ראה עבודות' }
  : { primary: 'Hire me', secondary: 'See work' };

const statusText = isHe
  ? 'פתוח לתפקידים · ישראל · מרחוק או היברידי'
  : 'Open to roles · Israel · Remote or hybrid';
---

<section id="cover" class="section page-shell pt-28 lg:pt-32">
  <p class="eyebrow mb-5">{heroContent.kicker[locale]}</p>

  <h1 class="display display-xl max-w-4xl">{headline}</h1>

  <p class="prose-lede mt-6 max-w-2xl">{heroContent.intro[locale]}</p>

  <div class="mt-8 flex flex-wrap items-center gap-3">
    <a href="#contact" class="btn btn-primary">
      {ctas.primary}
      <span aria-hidden="true">→</span>
    </a>
    <a href="#work" class="btn btn-ghost">
      {ctas.secondary}
    </a>
  </div>

  <p class="mt-7 inline-flex items-center gap-2 mono text-xs text-muted">
    <span class="inline-block h-1.5 w-1.5 rounded-full bg-accent" aria-hidden="true"></span>
    {statusText}
  </p>
</section>
