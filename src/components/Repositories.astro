---
import type { Locale } from '../i18n/index';
import { t } from '../i18n/index';
import { repositoryHighlights } from '../content/repositories';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
---

<section id="repositories" class="section-container">
  <h2 class="text-3xl md:text-4xl font-bold mb-4 text-center">
    <span class="gradient-text">{t(locale, 'repositories.title')}</span>
  </h2>
  <p class="text-slate-400 text-center mb-12 max-w-3xl mx-auto text-base md:text-lg leading-relaxed">
    {t(locale, 'repositories.subtitle')}
  </p>

  <div class="grid lg:grid-cols-3 gap-6">
    {repositoryHighlights.map((repo) => (
      <article class={`card group flex h-full flex-col ${repo.featured ? 'lg:col-span-1' : ''}`}>
        <div class="mb-4 flex items-start justify-between gap-4">
          <div>
            <h3 class="text-xl font-bold text-slate-100 group-hover:text-accent-light transition-colors">
              {repo.name}
            </h3>
          </div>
          <span class="rounded-full border border-accent/30 bg-accent/10 px-2.5 py-1 text-[11px] font-semibold uppercase tracking-wide text-accent-light">
            GitHub
          </span>
        </div>

        <p class="mb-5 flex-1 text-sm leading-relaxed text-slate-400">
          {repo.description[locale]}
        </p>

        <div class="mb-5 flex flex-wrap gap-2">
          {repo.tags.map((tag) => (
            <span class="rounded-md border border-slate-700/60 bg-surface-lighter/50 px-2 py-1 text-xs text-slate-300">
              {tag}
            </span>
          ))}
        </div>

        <div class="flex flex-wrap gap-3 border-t border-slate-700/50 pt-4">
          <a
            href={repo.github}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 rounded-lg bg-accent px-4 py-2 text-sm font-medium text-white transition-all hover:bg-accent-dark hover:shadow-lg hover:shadow-accent/25"
          >
            <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
            </svg>
            {t(locale, 'repositories.viewRepo')}
          </a>
          {repo.live && (
            <a
              href={repo.live}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center gap-2 rounded-lg border border-slate-600 px-4 py-2 text-sm font-medium text-slate-300 transition-all hover:border-accent hover:text-white"
            >
              <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
              </svg>
              {t(locale, 'projects.viewLive')}
            </a>
          )}
        </div>
      </article>
    ))}
  </div>
</section>
