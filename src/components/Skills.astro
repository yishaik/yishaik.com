---
import type { Locale } from '../i18n/index';
import { skillsContent } from '../content/site';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
---

<section id="skills" class="tile tile-skills">
  <div class="section-header reveal">
    <p class="section-label">03 / {skillsContent.label[locale]}</p>
    <h2 class="display display-lg mt-4 max-w-3xl">{skillsContent.heading[locale]}</h2>
    <p class="prose-body mt-4 max-w-2xl">{skillsContent.lede[locale]}</p>
  </div>

  <div class="grid gap-3">
    {skillsContent.categories.map((cat, i) => (
      <div class="card reveal" style={`--reveal-delay:${(i % 2) * 80}ms`}>
        <div class="flex items-baseline justify-between gap-3">
          <h3 class="display text-base font-semibold text-ink">{cat.name[locale]}</h3>
          <span class="mono text-[0.65rem] tracking-[0.14em] uppercase text-muted">
            {String(i + 1).padStart(2, '0')}
          </span>
        </div>
        <div class="mt-4 flex flex-wrap gap-1.5">
          {cat.skills.map((skill) => (
            <span class="tag tag-accent">{skill}</span>
          ))}
        </div>
      </div>
    ))}
  </div>
</section>
